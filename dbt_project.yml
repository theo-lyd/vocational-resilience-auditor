name: vocational_resilience_auditor
version: 1.0.0
config-version: 2

profile: vocational_resilience_auditor

model-paths: ["models"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

vars:
  bronze_dir: "data/bronze"

models:
  vocational_resilience_auditor:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
